# Debug build artifacts

Reserved for STM32CubeIDE Debug output for `timer_04_systick`. The local project contains generated build output; binary `.elf` and `.o` artifacts cannot be uploaded with the current GitHub connector.
