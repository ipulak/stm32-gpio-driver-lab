# Debug build artifacts

Reserved for STM32CubeIDE Debug output for `timer_06_capstone`. The local project includes generated `.elf` and `.o` files plus text build reports. The current GitHub connector can upload UTF-8 text but not binary build artifacts, so the original local Debug directory should be retained for exact debugger output.
